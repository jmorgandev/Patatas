#ifndef PTS_DIALOG_MEMORY
#define PTS_DIALOG_MEMORY
#pragma once
#include "stdafx.h"
#include <Windows.h>

INT_PTR CALLBACK DialogProc_Memory(HWND hwnd, UINT msg, WPARAM wParam, LPARAM lParam);

#endif