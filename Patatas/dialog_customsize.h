#ifndef PTS_CUSTOMSIZE_DIALOG
#define PTS_CUSTOMSIZE_DIALOG
#include <Windows.h>
#include "stdafx.h"

INT_PTR CALLBACK DialogProc_CustomSize(HWND hwnd, UINT msg, WPARAM wParam, LPARAM lParam);

#endif